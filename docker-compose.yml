services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: kloudkoach
      POSTGRES_PASSWORD: kloudkoach
      POSTGRES_DB: kloudkoach
    ports:
      - "5432:5432"
    volumes:
      - kloudkoach_pgdata:/var/lib/postgresql/data

volumes:
  kloudkoach_pgdata:
